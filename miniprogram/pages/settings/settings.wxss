/* settings.wxss - 我的/设置页面 Claymorphism 样式 */

.page-settings {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--clay-bg, #fffbf8);
}

.status-bar {
  flex-shrink: 0;
}

/* Navigation Bar */
.settings-nav {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 104rpx;
  flex-shrink: 0;
}

.nav-title-centered {
  font-size: var(--clay-font-xxl);
  font-weight: 700;
  color: var(--clay-text-primary);
}

/* Content Area */
.content-area {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 40rpx;
  padding: 40rpx 0 160rpx;
  width: 750rpx;
  box-sizing: border-box;
  overflow-y: auto;
}

/* User Card (v2) */
.user-card {
  width: 670rpx;
  height: 160rpx;
  background: #ffffff;
  border-radius: 48rpx;
  box-shadow: 0px 4px 12px 0px rgba(230, 198, 179, 0.3);
  display: flex;
  align-items: center;
  gap: 24rpx;
  flex-shrink: 0;
  box-sizing: border-box;
  padding: 0 32rpx;
}

.user-avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 48rpx;
  background: #dceaf1;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.user-avatar text {
  font-size: 48rpx;
  color: #4a4a4a;
}

.user-info {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.user-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #4a4a4a;
}

.user-record {
  font-size: 26rpx;
  font-weight: 400;
  color: #999;
}

.achievement-badge {
  width: 112rpx;
  height: 110rpx;
  background: var(--clay-card-beige);
  border-radius: 32rpx;
  box-shadow: 0px 4px 8px 0px rgba(244, 230, 214, 0.35);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4rpx;
  margin: 16rpx;
  flex-shrink: 0;
  box-sizing: border-box;
}

.badge-icon {
  font-size: 44rpx;
}

.badge-count {
  font-size: var(--clay-font-sm);
  font-weight: 600;
  color: var(--clay-text-dark-warm);
  text-align: center;
}

/* Stats Container (v2: single card with 3 columns) */
.stats-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  width: 670rpx;
  background: #ffffff;
  border-radius: 48rpx;
  box-shadow: 0px 4px 12px 0px rgba(230, 198, 179, 0.3);
}

.stat-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 16rpx 0;
  flex-shrink: 0;
}

.stat-value {
  font-size: 44rpx;
  font-weight: 700;
  color: #ffa87a;
}

.stat-label {
  font-size: 24rpx;
  font-weight: 400;
  color: #999;
}

/* Menu Items Container */
.menu-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
  width: 670rpx;
  background: #ffffff;
  border-radius: 48rpx;
  box-shadow: 0px 4px 12px 0px rgba(230, 198, 179, 0.3);
  flex-shrink: 0;
  box-sizing: border-box;
  overflow: hidden;
}

.menu-item {
  width: 100%;
  height: 136rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding: 0 32rpx;
  flex-shrink: 0;
  box-sizing: border-box;
  border-bottom: 1rpx solid #f5f0eb;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 28rpx;
  background: var(--clay-icon-bg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.menu-icon text {
  font-size: 40rpx;
}

.menu-text-area {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.menu-title {
  font-size: var(--clay-font-body);
  font-weight: 500;
  color: var(--clay-text-primary);
}

.menu-desc {
  font-size: var(--clay-font-sm);
  color: var(--clay-text-secondary);
}

.menu-arrow {
  font-size: 44rpx;
  color: var(--clay-text-muted);
  text-align: center;
  flex-shrink: 0;
  width: 44rpx;
}

/* ========== Developer Settings ========== */

.dev-divider {
  width: 670rpx;
  height: 2rpx;
  background: var(--clay-text-muted);
  opacity: 0.15;
  margin: 8rpx 0;
  flex-shrink: 0;
}

.dev-menu-item {
  opacity: 0.85;
}

.dev-icon {
  background: var(--clay-card-blue);
}

.dev-env-name {
  color: var(--clay-primary) !important;
  font-weight: 600 !important;
}

/* ========== Environment Picker Popup ========== */

.env-picker-container {
  background: var(--clay-card-white);
  border-radius: 48rpx 48rpx 0 0;
  padding: 40rpx 32rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom));
}

.env-picker-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 32rpx;
}

.env-picker-title {
  font-size: var(--clay-font-xxl);
  font-weight: 700;
  color: var(--clay-text-primary);
}

.env-picker-hint {
  font-size: var(--clay-font-sm);
  color: var(--clay-text-muted);
}

.env-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 32rpx;
}

.env-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 28rpx;
  border-radius: 28rpx;
  background: var(--clay-bg);
  box-shadow: var(--clay-shadow-card-light);
  transition: all 0.2s ease;
}

.env-item-active {
  background: var(--clay-icon-bg);
  box-shadow: 0px 0px 0px 3rpx var(--clay-primary);
}

.env-info {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.env-name {
  font-size: var(--clay-font-body);
  font-weight: 600;
  color: var(--clay-text-primary);
}

.env-desc {
  font-size: var(--clay-font-sm);
  color: var(--clay-text-secondary);
}

.env-check {
  width: 48rpx;
  height: 48rpx;
  border-radius: 24rpx;
  border: 3rpx solid var(--clay-text-muted);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.env-check text {
  font-size: 28rpx;
  color: transparent;
}

.env-check-active {
  background: var(--clay-primary);
  border-color: var(--clay-primary);
}

.env-check-active text {
  color: #fff;
}

.env-picker-actions {
  display: flex;
  gap: 24rpx;
}

.env-btn {
  flex: 1;
  height: 96rpx;
  border-radius: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--clay-font-body);
  font-weight: 600;
}

.env-btn-cancel {
  background: var(--clay-bg);
  color: var(--clay-text-secondary);
  box-shadow: var(--clay-shadow-card-light);
}

.env-btn-confirm {
  background: var(--clay-primary);
  color: #fff;
  box-shadow: 0px 4px 12px 0px rgba(255, 168, 122, 0.4);
}

/* ========== Locale Picker Popup ========== */

.locale-picker-container {
  background: var(--clay-card-white);
  border-radius: 48rpx 48rpx 0 0;
  padding: 40rpx 32rpx;
  padding-bottom: calc(40rpx + env(safe-area-inset-bottom));
}

.locale-picker-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 32rpx;
}

.locale-picker-title {
  font-size: var(--clay-font-xxl);
  font-weight: 700;
  color: var(--clay-text-primary);
}

.locale-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 32rpx;
}

.locale-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 28rpx;
  border-radius: 28rpx;
  background: var(--clay-bg);
  box-shadow: var(--clay-shadow-card-light);
}

.locale-item-active {
  background: var(--clay-icon-bg);
  box-shadow: 0px 0px 0px 3rpx var(--clay-primary);
}

.locale-name {
  font-size: var(--clay-font-body);
  font-weight: 600;
  color: var(--clay-text-primary);
}

.locale-check {
  width: 48rpx;
  height: 48rpx;
  border-radius: 24rpx;
  border: 3rpx solid var(--clay-text-muted);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.locale-check text {
  font-size: 28rpx;
  color: transparent;
}

.locale-check-active {
  background: var(--clay-primary);
  border-color: var(--clay-primary);
}

.locale-check-active text {
  color: #fff;
}

.locale-picker-actions {
  display: flex;
  justify-content: center;
}

.locale-btn {
  height: 96rpx;
  border-radius: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--clay-font-body);
  font-weight: 600;
  padding: 0 80rpx;
}

.locale-btn-cancel {
  background: var(--clay-bg);
  color: var(--clay-text-secondary);
  box-shadow: var(--clay-shadow-card-light);
}